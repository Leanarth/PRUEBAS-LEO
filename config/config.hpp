#pragma once

// Lee, carga datos del archivo .config y escribe el archivo .config
int configureData();  // Lee .config y rellena configurations[]
int loadConfig();     // Copia configurations[] a las barras del frontend en CONFIGURATIONS
